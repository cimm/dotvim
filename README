# Prerequisites

Don't forget to install a search replacement if you don't like grep. The Silver Searcher is ack compatible and faster:

    brew install ag

# Installation

    cd ~
    git clone http://github.com/cimm/dotvim.git ~/.vim
    ln -s ~/.vim/vimrc ~/.vimrc
    cd ~/.vim
    git submodule update --init --recursive

